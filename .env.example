# Copy to .env.local (gitignored) to prefill the OpenRouteService key during
# local development/testing. Leave blank in production — users enter their own
# key in the app, and it's stored only in their browser.
VITE_ORS_KEY=
